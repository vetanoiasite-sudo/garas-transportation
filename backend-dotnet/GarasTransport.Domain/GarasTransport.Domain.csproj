<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>GarasTransport.Domain</RootNamespace>
  </PropertyGroup>

  <!-- Domain layer: pure domain logic — NO project references (Clean Architecture rule). -->

</Project>
