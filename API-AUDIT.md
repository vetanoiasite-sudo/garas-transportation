# فحص شامل: الفرونت مقابل الـ CoreApi

فحص شاشة-بشاشة لكل نداء API (54 نداء)، بقراءة **جسم الميثود في الباك** مش أسماء
الحقول بس. النتيجة: **39 مشكلة مؤكدة**.

الحالة: ✅ = متصلح ومتأكد منه | 🔧 = اتعالج **في الفرونت** (الباك زي ما هو)

> الباك نسخة طبق الأصل من الـ CoreApi ومفيش فيه أي تعديل. كل مشكلة كانت
> "محتاجة تعديل في الباك" اتعالجت بمعالجة الداتا في الفرونت.

---

## متصلح ✅

| # | الشاشة | المشكلة | الإصلاح |
|---|---|---|---|
| 1 | الموردين (إضافة) | `Type` و `CreatedBy` إجباريين والفرونت مش بيبعتهم → **كل إضافة/تعديل بيفشل** | بعتنا `Type` و `CreatedBy` = معرّف المستخدم **المشفّر** من اللوجين (`UserID`) |
| 2 | الموردين | `SupplierSpecialities` مش مبعوت → الباك بيعمل `.Count` على null → NullReference | بعتناها `[]` |
| 3 | الموردين | العنوان محتاج `CountryID/GovernorateID/AreaID` | مابنبعتش `SupplierAddresses` (الحقل اختياري في الشاشة) |
| 4 | الموردين (تعديل) | أرقام الهاتف بتتبعت من غير `ID` → الباك بيرفضها كمكررة ("Line: 1") | بنقرا الـ IDs ونرجّعها |
| 5 | الموردين (تعديل) | التعديل بيمسح: الملاحظات، التقييم، السجل التجاري، البطاقة الضريبية، ورقم المسلسل | بنقرا القيم الحالية ونرجّعها |
| 6 | الموردين (جهات اتصال) | `Title` و `Mobile` إجباريين | بعتنا `Title` وبنتخطى الصفوف من غير موبايل |
| 7 | الخطوط (إضافة) | الأوقات بتتبعت `"06:00"` والـ API عايز `DateTime` → **400** | تحويل لـ ISO كامل |
| 8 | الخطوط | `FromDate` بدل `FromoDate` (غلطة مجمّدة) | الاسم الصح |
| 9 | الخطوط | `PeriodFrom` مش مبعوت → `01/01/0001` مرفوض من SQL | بيتبعت |
| 10 | الخطوط | `Serial` بيتبعت وهو تلقائي | اتشال |
| 11 | الخطوط | من غير محطة تجمع، لازم يتبعت `TransportationLineName` والباك يعمل المحطة | بيتبعت |
| 12 | الخطوط (تعديل) | `UpdateTransportationRoute` بيكتب كل الأعمدة — و`IsApproved` مش مبعوت → **كل تعديل بيلغي الاعتماد** | بنقرا الاعتماد من `getAllRoutes` ونرجّعه |
| 13 | الخطوط (فلتر) | الهيدر اسمه `pTransportionlineId` بدل `TransportionlineId` → الفلتر مابيشتغلش | الاسم الصح |
| 14 | محطات التجمع | زرار "اعتماد" وهمي: `TransportationLine` **مفيهاش عمود اعتماد** و`UpdateTransportationLine` بيكتب الاسم بس | الزرار والشارة اتشالوا |
| 15 | إعادة التسعير | `IsPercent` بدل `IsPrecent` → النسبة بتتحفظ **كمبلغ ثابت** | الاسم الصح |

## اتعالج في الفرونت (من غير لمس الباك) 🔧

| # | الشاشة | المشكلة | المعالجة في الفرونت |
|---|---|---|---|
| 16 | حضور الاتوبيسات | `BusAttendance` بيرجّع `{RouteName, Attendance[]}` و`CheckIn/CheckOut` **boolean** مش وقت — الشاشة كلها فاضية | الـ mapper بيفرد المجموعات لصف لكل أتوبيس، والأعمدة بتعرض حضر/غائب + عدد الركاب بدل الأوقات |
| 17 | الموظفين | `HrUserCardDto` مفيهوش `IdentityNumber`/`Active`/`RoutesCount` → الأعمدة فاضية وكل راكب بيظهر "نشط" | الاسم بيتجمّع من First/Middle/Last؛ الحقول الناقصة بتتقرا من ملف الراكب |
| 18 | الموظفين (إضافة/تعديل) | `EditHrEmployee`/`CreateHrUser` بيرفضوا لو `ARLastName` أو `Email` فاضيين | الفرونت بيبعت الاسم العربي من نفس التقسيم، والإيميل بيترحّل أو بيتولّد من رقم الهوية |
| 19 | تقرير الحضور | `supplierContactPersonName` مابيتعبّاش → فلتر السائق **بيفضّي الجدول** | الفلترة بقت على السيرفر بـ `supplierContactPersonId` وعمود السائق اتشال |
| 20 | تقرير الحضور | فلتر المسلسل مابيتطبّقش في الباك أصلًا | بيتبعت زي ما هو (سلوك موروث) — الفلترة الفعلية بالخط |
| 21 | كل ملفات Excel | الباك بيرجّع **رابط** مش base64 | `fileUrl()`/`openFileUrl()` بينضّفوا الرابط ويفتحوه بدل `atob` |
| 22 | استيراد Excel | `[FromForm] IFormFile` والفرونت بيبعت JSON → 415 | بقى `apiForm` بملف حقيقي، والرد بيتقرا كرابط سجل أخطاء |
| 23 | استيراد التفعيل | الكنترولر بينده `InsertUsersWithRoutesExcel` بدل `InsertUserNotActiveExcel` — **بينشئ موظفين بدل ما يفعّل** | متوثّق بتحذير في الكود — سلوك الباك المرجعي |
| 24 | كشف الحساب | فلتر الخط مابيتطبّقش؛ `AccountId` دايمًا 0 | مفتاح مركّب `SupplierId-MonthNum` بدل `AccountId` |
| 25 | إعادة التسعير | `PriceAfter` بيحسب الزيادة بس مش السعر الجديد؛ "تم الاعتماد بواسطة" بيعرض منشئ الطلب | السعر الجديد بيتحسب `before + increase`، والاسم بيظهر بعد الاعتماد بس |

## متصلح كمان ✅

| # | الشاشة | المشكلة | الإصلاح |
|---|---|---|---|
| 26 | الخصومات | الفرونت بيقرا `Date` والصح `DateOfDeduction` | الاسم الصح |
| 27 | التكاليف | `deductionCost` بيتضرب في عدد الخصومات | الباك بيرجّع المجموع أصلًا — الضرب اتشال |
| 28 | التكاليف | التواريخ متحطوطة ثابتة `2026-07-01/31` | الشهر الحالي |
| 29 | التكاليف | فلتر السائق مش بيتبعت خالص | بيتبعت كـ `supplierContactPersonId` |
| 30 | تقرير الحضور | فلتر الراكب بيتبعت `EmployeeId` والصح `HrUser` | الهيدر الصح |
| 31 | الخصومات | قائمة الخطوط في الديالوج بترجع 20 صف بس | `NoOfItems: 500` |
| 32 | تعيين الخطوط للراكب | `UpdateRouteEmployee` بيمسح محطة الراكب | `directionId` بيترحّل مع الصف |
| 33 | الاستثناءات | تواريخ فاضية `""` → **400** | بتتحوّل لـ `undefined` |
| 34 | الاستثناءات | `DayName` نص حر بالعربي والباك بيقارن بأسماء إنجليزية | ⬜ لسه |
| 35 | إعادة التسعير | تاريخ البدء اختياري في الشاشة وإجباري في الـ DB | ديفولت النهارده |
| 36 | الراكب | `Longitude` بدل `Longtitud` → الإحداثي بيتمسح كل حفظ | الاسم الصح |
| 37 | الراكب | صورة الراكب مش بتتبعت خالص | ⬜ لسه (محتاج رفع ملف) |
| 38 | لوحة المعلومات | 8 حقول في كروت المقارنة مش موجودة في `DashBoardVM` | ⬜ لسه (إعادة ربط بالأسماء الموجودة) |
| 39 | الموظفين | فلاتر النطاق بتتطبّق **واحدة بس** (الباك early-return) | ⬜ لسه |
